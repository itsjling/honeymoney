# Implementation Plans

Generated by the `improve` skill on 2026-07-11 against commit `aa0eedf`.
Execute in the order below unless dependencies say otherwise. Each executor
must read its plan fully, honor STOP conditions, run every verification gate,
and update its status row.

The repository already had unrelated working-tree changes when these plans
were written. Preserve them. Never use private statements from `samples/`,
`private_samples/`, local money workspaces, or generated outputs in tests,
commits, logs, issues, or prompts.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| [001](001-preserve-failed-replacements.md) | Preserve failed replacement rows | P1 | S | — | DONE (`e8ae14c`) |
| [002](002-validate-public-config.md) | Validate public config | P1 | M | — | DONE (`34e3d19`, `8ca7813`) |
| [003](003-validate-profile-structure.md) | Validate profile structure | P1 | M | 002 | DONE (`d5543ac`) |
| [004](004-define-empty-corrections.md) | Define empty correction semantics | P1 | S | 002 | DONE (`b989a5c`) |
| [005](005-failure-atomic-persistence.md) | Make persistence recoverable | P1 | L | 001, 004 | DONE (`c59682f`, `9f71419`, `fa892cf`, `8a2af0c`) |
| [006](006-transactional-reset.md) | Make reset transactional | P1 | M | 005 | DONE (`655c704`) |
| [007](007-enforce-local-ollama.md) | Enforce local-only Ollama | P1 | M | 002 | DONE (`5d6711a`) |
| [008](008-stable-transaction-identity.md) | Stabilize transaction identity | P1 | L | 001, 005, 006 | DONE (`2660ecc`) |
| [009](009-stable-source-namespace.md) | Stabilize source namespace | P1 | M | 008 | DONE (`691c6e8`) |
| [010](010-cross-import-duplicates.md) | Detect cross-import duplicates | P2 | M | 008, 009 | DONE (`ab45c05`) |
| [011](011-optimize-duplicate-window.md) | Optimize duplicate scanning | P2 | M | 010 | DONE (`11ddae3`) |
| [012](012-safe-spreadsheet-exports.md) | Make CSV exports spreadsheet-safe | P2 | M | 005 | DONE (`0ba6a9e`) |
| [013](013-pin-ci-toolchain.md) | Stabilize CI dependency resolution | P3 | S | — | DONE (`8399d79`) |
| [014](014-single-ledger-read.md) | Read ledger once per import | P3 | S | 005, 009 | DONE (`6a6d8bb`) |
| [015](015-local-categorization-memory.md) | Add local categorization memory | P2 | M | 004, 008 | DONE (`9c2e6c3`) |
| [016](016-profile-validation-command.md) | Add profile validation tooling | P2 | M | 003 | DONE (`9495a54`, `4cb40da`) |
| [017](017-extract-cli-boundaries.md) | Extract CLI module boundaries | P3 | L | 001–016 | DONE (`50522e3`) |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED` (with reason), or
`REJECTED` (with rationale).

## Dependency notes

- 005 establishes the persistence commit/recovery boundary used by 006 and 012.
- 008 must settle stable transaction occurrence identity before 009, 010, or
  correction-derived categorization memory rely on it.
- 009 separates human-readable source traceability from replacement identity.
- 010 establishes correct cumulative duplicate behavior before 011 optimizes it.
- 003 exposes reusable profile validation before 016 adds a public diagnostic command.
- 017 runs last so it extracts stable behavior rather than moving actively changing code.
- Independent work that can proceed early: 001, 002, 007 after 002, and 013.

## Verification baseline

- Focused suites are named in each plan.
- Required final gate for every implementation plan: `./scripts/check.sh`.
- This runs Ruff format check, Ruff lint, all unittests, and a package build.
- Live Ollama smoke testing is excluded unless explicitly requested.

## Findings considered and rejected

- **HTML report script injection**: rejected. `honeymoney/report.py` JSON-encodes
  data, escapes `</`, and inserts values with DOM `textContent`; no unsafe HTML
  sink was found.
- **Missing `CONTEXT.md` or ADR directory**: rejected. `docs/agents/domain.md`
  explicitly says their absence is not a finding; decisions should be added
  only when a selected plan actually resolves one.
- **Replace filesystem storage with a database**: rejected. The local
  filesystem boundary is an explicit architectural choice; Plan 005 hardens it.
- **Cloud categorization or sync**: rejected. It conflicts with the core
  privacy boundary and v1 exclusions.

## Audit scope recorded for reconciliation

The audit covered all Python source modules, key synthetic tests and fixtures,
packaging/CI configuration, README/spec/architecture documentation, and recent
git churn. It did not inspect private statements, generated outputs, live
Ollama behavior, external dependency advisories, or mutate/build the user's
working tree. At planning time, 141 unit tests and Ruff checks passed.
